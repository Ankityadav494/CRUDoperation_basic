<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center justify-center">
    <h1 class="text-3xl font-extrabold text-blue-700 mb-8 drop-shadow-lg text-center">About Users</h1>
    <a href="/" class="text-blue-600 mb-8 drop-shadow-lg text-center">Back to Home</a>
    <div class="flex flex-wrap gap-8 mb-4 p-8 justify-center w-full">
        
       <% users.forEach(function(user){ %>
        <div class="max-w-md w-full bg-white p-8 rounded-lg shadow-lg flex flex-col items-center">
            <img src="<%= user.image_url%>" alt="img of user" class="w-24 h-24 rounded-full bg-gray-200 mb-4 object-cover" />
            <h3 class="text-xl font-semibold text-gray-800 mb-1"><%=user.name%></h3>
            <h5 class="text-gray-500 mb-1"><%=user.email%></h5>
          
            <div class="flex gap-4">
                <a href="/edit/<%=user._id%>" class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition">Edit</a>
                <a href="/delete/<%=user._id%>" class="px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition">Delete</a>
            </div>
        </div>
            <%});%>
            
    
    
    </div>
       
    </div>
</body>
</html>
