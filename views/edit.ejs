<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center justify-center">
    <h1 class="text-3xl font-extrabold text-blue-700 mb-8 drop-shadow-lg text-center">Edit User</h1>
    
            
  <form action="/update/<%=user._id%>" method="POST" class="space-y-4">
            <input value="<%=user.name%>" name="name" type="text" placeholder="Name" class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
            <input value="<%=user.email%>" type="email" name="email" placeholder="Email" class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
        
            <input value="<%=user.image_url%>" type="text" name="image_url" placeholder="image_url" class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
            <input type="submit" value="Update now " class="w-full bg-yellow-500 text-white py-2 rounded hover:bg-yellow-400 cursor-pointer font-semibold" />
        </form>

</body>
</html>
